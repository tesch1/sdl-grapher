Run:
	sh build.sh
to build the library without installing.

Run:
        sh build.sh install
to build the library and install the library and header in
/usr/lib and /usr/include respectively.

You will also need the development package of SDL installed.
